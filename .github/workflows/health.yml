name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run SSH command
        uses: appleboy/ssh-action@master
        with:
          script: |
            echo "Hello, World!"
